# 第一阶段测试

更新时间：2026-03-25

本文档只记录已经实际执行并确认通过的测试项。

## 已通过测试

### 1. 后端 Python 语法编译检查

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：

```powershell
python -m compileall backend
```

- 结果：通过
- 说明：`backend/`、`backend/diarization/`、`backend/engines/`、`backend/postprocess/` 均完成编译检查，未出现阻断错误。

### 2. Tauri 前端 TypeScript + Vite 生产构建

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app`
- 执行命令：

```powershell
npm run build
```

- 结果：通过
- 说明：执行链路为 `tsc --noEmit && vite build`，前端构建成功，生成 `dist/` 输出。

### 3. Rust Cargo 可执行文件可用性验证

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：

```powershell
& "C:\Program Files\Rust stable MSVC 1.94\bin\cargo.exe" --version
```

- 结果：通过
- 输出结果：

```text
cargo 1.94.0 (85eff7c80 2026-01-15)
```

- 说明：Rust/Cargo 已安装成功。

### 4. Tauri Rust 工程 `cargo check`

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app\src-tauri`
- 执行命令：

```powershell
cmd /c "set PATH=C:\Program Files\Rust stable MSVC 1.94\bin;%PATH% && call D:\VSBuildTools2022\Common7\Tools\VsDevCmd.bat -no_logo && cargo check"
```

- 结果：通过
- 说明：
- 已安装 Visual Studio Build Tools 到 `D:\VSBuildTools2022`
- 已安装缓存目录到 `D:\VSBuildToolsCache`
- 已安装共享目录到 `D:\VSBuildToolsShared`
- `link.exe` 已可用

### 5. Tauri 生产打包 `npm run tauri:build`

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app`
- 执行命令：

```powershell
cmd /c "set PATH=C:\Program Files\Rust stable MSVC 1.94\bin;%PATH% && call D:\VSBuildTools2022\Common7\Tools\VsDevCmd.bat -no_logo && npm run tauri:build"
```

- 结果：通过
- 说明：
- 已生成可执行文件 `tauri-app/src-tauri/target/release/voicescribe-desktop.exe`
- 已生成 NSIS 安装包 `tauri-app/src-tauri/target/release/bundle/nsis/VoiceScribe_0.2.0_x64-setup.exe`

### 6. 启动脚本验证

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：

```powershell
cmd /c scripts\start_windows_system.bat
```

- 结果：通过
- 说明：
- 脚本返回码为 `0`
- 启动后进程 `voicescribe-desktop.exe` 已存在
- 主窗口标题为 `VoiceScribe`

### 7. 桌面应用启动后后端自动拉起

- 验证方式：启动 `scripts\start_windows_system.bat` 后检查进程树
- 结果：通过
- 说明：
- `voicescribe-desktop.exe` 已启动
- `python.exe` 作为其子进程启动
- 后端命令行为 `python ...\\backend\\server.py`

### 8. 后端健康检查与引擎列表接口

- 执行命令：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8765/health
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8765/engines
```

- 结果：通过
- 说明：
- `/health` 返回 `status=healthy`
- `/engines` 返回 Whisper、WhisperCpp、FunASR、Parakeet 引擎列表

### 9. 模型与说话人接口联调

- 执行命令：

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8765/models
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8765/speakers
```

- 结果：通过
- 说明：
- `/models` 返回当前模型状态列表
- `/speakers` 返回当前说话人列表

### 10. 开发态读取仓库根目录 `models/` 回归验证

- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\backend`
- 执行命令：

```powershell
$env:VOICESCRIBE_RUNTIME_DIR='D:\learn\AIGC\voicescribe\0324\voicescribe'
$env:VOICESCRIBE_MODEL_DIR='D:\learn\AIGC\voicescribe\0324\voicescribe\models'
python -c "from server import list_models; import asyncio, json; print(json.dumps([item.model_dump() for item in asyncio.run(list_models())], ensure_ascii=False, indent=2))"
```

- 结果：通过
- 说明：
- 旧注册表中的历史绝对路径会自动 rebasing 到当前仓库 `models/`
- `paraformer-zh`
- `paraformer-zh-streaming`
- `seaco-paraformer`
- `sensevoice-small`
- 以上 4 个 FunASR 模型均返回 `available=true`
