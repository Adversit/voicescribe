# 2026-03-29 录音悬浮窗与录音流专题测试报告
更新日期：2026-03-29
适用分支：`0325main`

## 当前状态

本文档记录本专题的构建验证、启动验证和人工验收结果。

## 自动验证

- `npm run build`：通过
- `cargo check`：通过
- `cmd /c scripts\start_windows_system.bat --skip-build`：通过
- `GET http://127.0.0.1:8765/health`：通过，`status=healthy`，`mock_mode=false`
- 代码链路已改为：悬浮窗波纹只使用 Rust 录音时的真实 `audio-level` 事件。

## 待人工验收

1. 快捷键首次触发后，悬浮窗是否稳定出现。
2. 悬浮窗显示后，再按一次快捷键是否能停止录音。
3. 点击取消 / 停止按钮是否能真实生效。
4. 波纹是否明显跟随真实麦克风输入。
5. 录音中 / 转录中 / 已取消 三种视觉态是否正确。
