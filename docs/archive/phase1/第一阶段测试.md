# 第一阶段测试

更新时间：2026-03-29

本文档只记录已经实际执行并确认通过的测试项。
没有写进本文档的，一律视为没测。

## 本轮功能修复

### 1. 托盘行为与图标
- 主窗口关闭时不再直接退出，而是隐藏到托盘。
- 托盘菜单保留“显示主窗口”和“退出 VoiceScribe”。
- Tauri 托盘已绑定应用图标资源。
- 说明：托盘图标是否在 Windows 托盘区正常显示，仍需要人工目视确认；代码和构建链路已通过。

### 2. FunASR 模型口径统一
- `/engines` 中 FunASR 的可选模型统一为 4 个：`paraformer-zh`、`paraformer-zh-streaming`、`seaco-paraformer`、`sensevoice-small`。
- `/models` 返回的 FunASR 模型状态也统一为这 4 个。
- 引擎页会按完整模型清单展示，未下载模型显示为“未下载”并保留下载按钮。

### 3. 模型目录统一到项目根目录 `models/`
- 模型下载目录固定为项目根目录 `models/`。
- 模型读取目录固定为项目根目录 `models/`。
- 历史注册表中的旧绝对路径会自动 rebasing 到当前项目的 `models/`。

## 已通过测试

### 1. 后端 Python 语法编译检查
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：`python -m compileall backend`
- 结果：通过

### 2. Tauri 前端 TypeScript + Vite 生产构建
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app`
- 执行命令：`npm run build`
- 结果：通过

### 3. Rust Cargo 可执行文件可用性验证
- 执行命令：`cargo --version`
- 结果：通过

### 4. Tauri Rust 工程 `cargo check`
- 执行命令：`cargo check`
- 结果：通过

### 5. Tauri 生产打包 `npm run tauri:build`
- 执行命令：`npm run tauri:build`
- 结果：通过

### 6. 启动脚本验证
- 执行命令：`cmd /c scripts\start_windows_system.bat`
- 结果：通过

### 7. 桌面应用启动后后端自动拉起
- 验证方式：启动后检查进程树。
- 结果：通过

### 8. 后端健康检查与引擎列表接口
- 执行命令：`GET /health`、`GET /engines`
- 结果：通过

### 9. 模型与说话人接口联调
- 执行命令：`GET /models`、`GET /speakers`
- 结果：通过

### 10. 开发态读取仓库根目录 `models/` 回归验证
- 执行方式：传入 `VOICESCRIBE_RUNTIME_DIR` 与 `VOICESCRIBE_MODEL_DIR` 后调用 `list_models()`。
- 结果：通过

### 11. 引擎页未下载模型展示与下载入口回归
- 执行命令：`npm run build`
- 结果：通过

### 12. 重启后接口状态回归验证
- 执行方式：重启系统后检查 `/engines` 与 `/models`。
- 结果：通过

### 13. FunASR 本地模型缺失注册表时的自愈识别
- 执行方式：删除 `models/voicescribe_models.json` 后执行 `list_models()`。
- 结果：通过

### 14. 历史旧绝对路径自动 rebasing 到当前 `models/`
- 执行方式：写入旧仓库绝对路径后执行 `list_models()`。
- 结果：通过

### 15. WhisperCpp `tiny` 真实下载/删除主链路验证
- 执行方式：调用 `POST /models/download` 与 `POST /models/delete`。
- 结果：通过

### 16. Windows 嵌入式 Python 占位脚本验证
- 执行命令：`cmd /c scripts\build_embedded_python.bat`
- 结果：通过

### 17. Embedded Python / 首次初始化闭环回归验证
- 执行方式：清空 `backend/venv` 后执行 `scripts\start_windows_system.bat`。
- 结果：通过

### 18. Windows 依赖安装脚本帮助输出验证
- 执行命令：`cmd /c scripts\install_windows_dependencies.bat --help`
- 结果：通过

### 19. Windows 全量依赖安装脚本验证
- 执行命令：`cmd /c scripts\install_windows_dependencies.bat --full`
- 结果：通过

### 20. Windows 启动脚本本地重建并启动桌面应用验证
- 执行命令：`cmd /c scripts\start_windows_system.bat`
- 结果：通过

### 21. Windows 启动脚本改为 `tauri build --no-bundle` 后的后端联通验证
- 执行命令：`cmd /c scripts\start_windows_system.bat`，随后检查 `/health`。
- 结果：通过

### 22. TypeScript 类型与默认值对齐验证
- 执行命令：`npm run build`
- 结果：通过

### 23. Tauri 插件接入与设置持久化迁移验证
- 执行命令：`npm run build`、`cargo check`
- 结果：通过

### 24. Embedded Python 准备脚本与 `venv` 能力回退验证
- 执行命令：`cargo check`、`cmd /c scripts\build_embedded_python.bat`
- 结果：通过

### 25. GitHub Actions Windows 构建 workflow 静态校验
- 执行方式：检查 `.github/workflows/build-windows.yml`。
- 结果：通过

### 26. Windows 开机自启动代码链路构建验证
- 执行命令：`npm run build`、`cargo check`
- 结果：通过

### 27. 主窗口结构向原版设置窗口收敛后的前端构建验证
- 执行命令：`npm run build`
- 结果：通过

### 28. 托盘菜单事件桥与前端监听构建验证
- 执行命令：`npm run build`、`cargo check`
- 结果：通过

### 29. embedded Python 自动解压与运行时目录辅助逻辑验证
- 执行命令：`cmd /c scripts\build_embedded_python.bat`、`cargo test --lib`、`cargo check`
- 结果：通过

### 30. 录音悬浮窗窗口化与状态桥接构建验证
- 执行命令：`npm run build`、`cargo check`
- 结果：通过

### 31. Release 桌面启动与后端健康检查验证
- 执行命令：`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 32. 当前未提交改动综合回归验证
- 执行命令：`python -m compileall backend`、`npm run build`、`cargo test --lib`、`cargo check`
- 结果：通过

### 33. 当前工作区 release 启动与后端健康检查回归验证
- 执行命令：`cmd /c scripts\start_windows_system.bat --skip-build`、`GET /health`
- 结果：通过

### 34. P4-01 侧边栏主窗口布局回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 35. P4-01 主窗口顶部摘要层移除回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 36. P4-01 单层主容器布局回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 37. P4-01 单层主体与高度压缩回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`、`Get-Process voicescribe-desktop`
- 结果：通过

### 38. 设置页信息密度与局部滚动收紧回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 39. 界面模仿计划第一轮落地回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`、`Get-Process voicescribe-desktop`
- 结果：通过

### 40. 界面 UTF-8 编码修复与侧边栏底部版本区移除回归验证
- 执行命令：`npm run build`、检查 bundle 中文、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 41. 单层主表面与区间式自适应窗口回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`、`Get-Process voicescribe-desktop`
- 结果：通过

### 42. 单层主表面与通用页重构回归验证
- 执行命令：`npm run build`、`cmd /c scripts\start_windows_system.bat`、`GET /health`
- 结果：通过

### 43. UTF-8 文档与通用页中文恢复回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：
  - `npm run build`
  - `cmd /c scripts\start_windows_system.bat`
  - `GET /health`
  - 检查 `tauri-app/dist/assets/main-B0bmHkCx.js` 中包含“通用 / 语言 / 输出方式 / 默认语言 / 后端连接状态”
- 结果：通过
- 说明：
  - 当前前端源码已全部恢复为合法 UTF-8。
  - 新 bundle 中已能检索到正常中文字符串，不再是 `\u` 字面量或乱码文本。
  - 桌面进程 `voicescribe-desktop.exe` 已正常启动，后端 `/health` 返回 `healthy`。
  - 本轮验证说明“通用”页乱码问题已在源码与构建产物层面修复；最终界面观感仍可由人工再确认一次。

### 44. 安装态模拟与 FunASR SyntaxWarning 回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：
  - `python -W error::SyntaxWarning -m py_compile backend/engines/funasr_engine.py`
  - `cargo check`
  - `cargo test --lib`
  - `npm run build`
  - `cmd /c scripts\start_windows_system.bat --packaged-test`
  - `GET /health`
- 结果：通过
- 说明：
  - `backend/engines/funasr_engine.py` 中的中文标点正则已改为不会触发 `SyntaxWarning` 的写法。
  - 安装态模拟下，桌面端能够把资源目录中的 `backend/` 同步到 `.packaged-runtime/backend`，并在 `.packaged-runtime/backend/venv` 下创建运行时 Python 环境。
  - 本轮实测时 `/health` 返回 `healthy`，且 `mock_mode=true`，说明“运行时目录 + 后端同步 + 最小依赖可启动”的最小闭环已经跑通。
  - 当前仓库仍未携带真实 `python-embed/python.exe` 或 `python-embed.zip`，因此这次验证使用的是系统 Python 回退链路，不等于“真实 embedded payload 已完成最终验证”。

### 45. 实时转录、历史记录与快捷键录制专题代码构建回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：
  - `python -m py_compile backend/server.py backend/config.py backend/postprocess/ai_refiner.py backend/engines/funasr_engine.py`
  - `npm run build`
  - `cargo check`
- 结果：通过
- 说明：
  - 后端 `/history`、`/summary`、`/stream` 相关代码与桌面端实时转录/历史记录/快捷键录制相关代码均通过当前代码层构建检查。

### 46. 历史记录 API 主链路烟测
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 先通过 `cmd /c scripts\start_windows_system.bat` 启动当前桌面端与后端
  - 然后对 `http://127.0.0.1:8765` 执行 `DELETE /history`、`POST /history`、`GET /history`、`GET /history/{record_id}/download/text`、`DELETE /history/{record_id}`、`DELETE /history`
- 结果：通过
- 说明：
  - 已验证历史记录新增、查询、文本导出、删除单条、清空全部的后端主链路可用。
  - 本轮烟测使用 `record_id=rt-history-test`。

### 47. AI 摘要接口烟测
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：
  - `POST /summary`
- 结果：通过
- 说明：
  - 已验证后端能返回 AI 摘要结果。
  - 当前在 mock/最小依赖场景下，允许走 `AIRefiner.summarize()` 或回退摘要逻辑。

### 48. `/stream` WebSocket 实时转录烟测
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 连接 `ws://127.0.0.1:8765/stream`
  - 发送 16kHz / 16-bit / mono PCM 模拟音频块
  - 读取第一条返回消息
- 结果：通过
- 说明：
  - 已收到 `type=entry` 的实时片段消息。
  - 当前 mock 模式下返回的示例内容为 `"[Mock] 正在实时转录..."`，说明实时转录页所需的基础流式数据契约已经打通。

### 49. 实时转录专题桌面启动与健康检查回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行命令：
  - `cmd /c scripts\start_windows_system.bat`
  - `GET /health`
  - `Get-Process voicescribe-desktop`
- 结果：通过
- 说明：
  - 当前桌面进程 `voicescribe-desktop.exe` 已正常启动。
  - 本轮实测 `/health` 返回 `healthy`，并显示 `mock_mode=true`。
  - 说明新增侧边栏页面与相关业务逻辑已集成进当前桌面应用启动链。

## 2026-03-29 补充测试记录

### 50. 热键专题前端构建回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app`
- 执行方式：
  - `npm run build`
- 结果：通过
- 说明：热键专题相关前端改动已完成 TypeScript 检查与生产构建。

### 51. Rust 全局热键命令与 Hook 链路静态检查
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe\tauri-app\src-tauri`
- 执行方式：
  - `cargo check`
- 结果：通过
- 说明：热键命令、全局 Hook 与事件桥接的 Rust 侧代码可正常编译。

### 52. Windows 桌面启动链热键日志落盘验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `cmd /c scripts\start_windows_system.bat`
  - `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果：通过
- 说明：桌面端启动后可稳定生成热键日志文件，便于继续定位热键链路问题。

### 53. ACL 权限补全后主窗口事件监听回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `npm run build`
  - `cargo check`
  - 重启桌面端并检查热键日志
- 结果：通过
- 说明：补齐 `main` 窗口事件监听所需 capability 后，不再出现 `plugin:event|listen not allowed by ACL`，热键与托盘监听均可注册成功。

### 54. `Right Alt` 双击触发链路日志验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 将快捷键设置为 `Right Alt`
  - 手动双击 `Right Alt`
  - 检查 `%TEMP%\voicescribe-hotkey.log`
- 结果：部分通过
- 说明：日志确认 `double_click -> start -> frontend beginRecordingSession start` 链路已命中；但后续录音仍因设备流配置不兼容而失败。

### 55. `Right Alt` 单键热键真实触发人工验收
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 保存 `Right Alt` 快捷键
  - 重启应用后手动按下 `Right Alt`
  - 检查设置文件与热键日志
- 结果：部分通过
- 说明：设置持久化与热键注册均为 `Right Alt`，日志确认已命中 `matches_hotkey vk=165`；当前剩余阻塞点不在热键，而在录音设备配置。

### 56. 快捷键交互切换为“单击开始，再按一次停止”静态验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 修改 Rust 热键状态机
  - `cargo check`
  - `npm run build`
- 结果：通过
- 说明：已将持续录音模式从“双击开始 / 再双击停止”改为“单击开始 / 再按一次停止”，长按模式保留。

### 57. 快捷键设置页说明文案同步与桌面重启回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 更新快捷键设置页文案
  - `npm run build`
  - `cmd /c scripts\start_windows_system.bat`
- 结果：通过
- 说明：设置页文案已同步为“单击模式 / 单击开始持续录音，再按一次停止”，并在重启后生效。

### 58. Windows 启动脚本后端彻底重启回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `cmd /c scripts\start_windows_system.bat --skip-build`
  - `curl.exe http://127.0.0.1:8765/health`
- 结果：通过
- 说明：启动脚本已能清理旧后端并重启到新进程，`/health` 返回 `healthy`，`mock_mode=false`。

### 59. 热键日志链路扩展到前端录音入口验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 在热键与前端录音入口增加调试日志
  - 手动触发快捷键
  - 检查 `%TEMP%\voicescribe-hotkey.log`
- 结果：通过
- 说明：日志已可观察到 `frontend received hotkey-start-recording` 与 `frontend beginRecordingSession start`，便于继续定位录音阶段故障。

### 60. 麦克风设备原生配置回退录音链路静态验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 修改 `audio.rs`，改为按设备原生输入配置采集，再本地转为 `16kHz / mono`
  - `cargo check`
- 结果：通过
- 说明：Rust 侧录音链路已不再强行请求设备原生 `16kHz / mono`，为后续真实录音兼容性修复打下基础。

### 61. 说话人注册链路真实加载验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 生成测试音频 `.tmp-tests\speaker_a.wav`
  - `POST http://127.0.0.1:8877/speakers/register`
  - `GET http://127.0.0.1:8877/health`
- 结果：通过
- 说明：说话人注册成功，`speaker_verification_loaded=true`，说明说话人识别模型已真实加载并参与处理。

### 62. FunASR 转录模型与说话人映射验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `POST http://127.0.0.1:8877/transcribe`
  - 参数：`engine=funasr`、`model=seaco-paraformer`、`enable_diarization=true`
- 结果：通过
- 说明：FunASR 转录结果已能返回说话人标签，并能映射到已注册说话人名称。

### 63. 外部分离模型加载与使用验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 使用 `SpeakerDiarizer.ensure_diarization_loaded()`
  - 对 `dialog_16k.wav` 执行外部分离
- 结果：通过
- 说明：外部分离已切到 ModelScope segmentation-clustering pipeline，模型可真实加载并返回分离片段。

### 64. 默认桌面启动链模型运行时验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `cmd /c scripts\start_windows_system.bat --skip-build`
  - `GET http://127.0.0.1:8765/health`
  - `POST http://127.0.0.1:8765/transcribe`
- 结果：通过
- 说明：默认桌面启动链上的 8765 后端已能真实加载转录、说话人识别与分离相关模型并返回结果。

### 65. Whisper 直连 Windows 运行时验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 直接执行 `WhisperEngine().load('base').transcribe(...)`
  - 再执行 `POST http://127.0.0.1:8765/transcribe`，参数为 `engine=whisper`
- 结果：失败
- 说明：`faster-whisper / ctranslate2` 在当前 Windows 环境下发生 native 崩溃，退出码为 `-1073741819`，该问题已单独记入 bug log。

### 66. Whisper Windows 回退链路修复后验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 切换到 `openai-whisper` 回退实现
  - 再次执行直连转录与 `POST http://127.0.0.1:8765/transcribe`
- 结果：通过
- 说明：Whisper 在 Windows 上已通过回退链路恢复可用，不再被原 native 崩溃阻塞。

### 67. FunASR GPU 运行时切换与后端链路验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 将 `backend/venv` 中的 `torch / torchaudio` 从 CPU 版切换到 `2.5.1+cu124`
  - 执行 `torch.cuda.is_available()` 与 `FunASREngine().load('seaco-paraformer')`
- 结果：通过
- 说明：FunASR 加载日志已显示 `[FunASR] Using device: cuda:0`，说明 GPU 运行时切换成功。

### 68. 默认桌面启动链 CUDA 运行时确认
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - `cmd /c scripts\start_windows_system.bat --skip-build`
  - `GET http://127.0.0.1:8765/health`
- 结果：通过
- 说明：`/health.runtime_checks.torch.version` 已显示 `2.5.1+cu124`，默认桌面启动链已吃到 CUDA 版运行时。

### 69. 录音悬浮窗与录音流静态验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 在 `tauri-app` 下执行 `npm run build`
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
  - 执行 `cmd /c scripts\start_windows_system.bat --skip-build`
  - 执行 `GET http://127.0.0.1:8765/health`
- 结果：通过
- 说明：overlay 状态桥接已改为显式 `overlay-ready`、`overlay-stop-recording`、`overlay-cancel-recording` 事件；Rust `audio-level` 现在同时发往 `main` 和 `overlay`，悬浮窗波纹直接接入真实录音输入。当前结果只覆盖构建、启动和健康检查，人工验收仍待继续。

### 70. 模型运行时缓存根项目内收口验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 执行 `py_compile` 校验 `backend/config.py`、`backend/server.py`、`backend/engines/funasr_engine.py`、`backend/diarization/speaker.py`
  - 使用 `backend\venv\Scripts\python.exe` 输出 `MODELSCOPE_CACHE`、`HF_HOME`、`HUGGINGFACE_HUB_CACHE`、`HF_DATASETS_CACHE`、`TRANSFORMERS_CACHE`、`TORCH_HOME`
  - 使用 `backend\venv\Scripts\python.exe` 解析本地 ASR / VAD / 标点 / 说话人模型路径，并执行 `FunASREngine().load('seaco-paraformer', enable_diarization=True)`、`SpeakerDiarizer().ensure_speaker_verification_loaded()`、`SpeakerDiarizer().ensure_diarization_loaded()`
  - 对比 `C:\Users\DingK\.cache\modelscope`、`C:\Users\DingK\.cache\huggingface` 与仓库 `models/` 的最新修改时间
- 结果：通过
- 说明：运行时环境变量已全部指向仓库根 `models/` 下的项目内路径；本轮验证中 `USER_MODELSCOPE` 与 `USER_HF` 的时间戳增量均为 `0`，仓库 `models/` 时间戳有更新，说明当前主链路未再把模型活动写回 C 盘默认缓存。需要注意，ModelScope 仍会在仓库内形成 `models\models\...` 的嵌套目录，但它仍属于项目内本地缓存，不是用户目录默认缓存。

### 71. 录音过短收口与悬浮窗透明样式静态回归验证
- 执行目录：`D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式：
  - 在 `tauri-app` 下执行 `npm run build`
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 执行 `GET http://127.0.0.1:8765/health`
- 结果：通过
- 说明：前端已补充 FunASR 录音低于 1 秒时的前置拦截，避免直接进入长时间 `transcribing`；Rust `transcribe` 命令已改为对 4xx 响应立即失败，不再重复重试 30 次；overlay 页面已改为透明背景，录音态已收口为黑色胶囊、左侧叉号、右侧对勾。当前结果仅覆盖构建、启动和健康检查，真实“过短录音提示”和“悬浮窗显示效果”仍待人工验收。

### 72. 启动阶段后端未就绪时的连接兜底回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app` 下执行 `npm run build`
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 轮询 `GET http://127.0.0.1:8765/health`
- 结果: 通过
- 说明: 前端已补充启动后的后端 ready 轮询；浏览器层 `Failed to fetch` 已统一映射为中文提示“后端服务尚未就绪，请稍后重试”；历史记录、模型列表、说话人列表页面已增加 `backendConnected=false` 时的防抢跑保护。本轮结果覆盖构建、启动和健康检查，真实“冷启动打开界面时不再出现 Failed to fetch”仍待人工验收。

### 73. 模型与运行时缓存迁移到仓库 models 目录验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 执行 `py_compile` 校验 `backend/config.py`?`backend/server.py`?`backend/engines/funasr_engine.py`
  - 提权执行 `migrate_legacy_caches()`，将 `C:\Users\DingK\.cache\modelscope`?`C:\Users\DingK\.cache\huggingface` 与 `%LOCALAPPDATA%\Temp\jieba.cache` 迁移或清理到 `models/`
  - 在 `backend` 下使用 `venv\Scripts\python.exe` 执行 `jieba.initialize()`
  - 执行 `cmd /c scripts\start_windows_system.bat --skip-build`
  - 轮询 `GET http://127.0.0.1:8765/health`
- 结果: 通过
- 说明: 当前 `C:\Users\DingK\.cache\modelscope`?`C:\Users\DingK\.cache\huggingface` 和 `%LOCALAPPDATA%\Temp\jieba.cache` 均已不存在；`jieba` 已确认从 `D:\learn\AIGC\voicescribe\0324\voicescribe\models\jieba\jieba.cache` 加载；`/health.cache_paths` 已返回仓库 `models/`?`models/huggingface` 与 `models/jieba/jieba.cache` 路径。本轮实际完成了历史 C 盘缓存迁移与清理验证。

### 74. Windows `venv` 启动后端的父子进程表现验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 查看 `tauri-app/src-tauri/src/commands/backend.rs` 中的 `ensure_backend_venv()` 与 `start_backend()`
  - 执行 `backend\venv\Scripts\python.exe server.py --port 8877`
  - 使用 `Get-CimInstance Win32_Process` 检查 `server.py` 进程的父子关系
  - 使用 `Get-NetTCPConnection -LocalPort 8877` 检查实际监听者
  - 强制结束父进程后再次检查 8877 监听与子进程是否退出
- 结果: 通过
- 说明: 当前看到的 `backend\venv\Scripts\python.exe ... server.py` 与 `D:\Anaconda3\python.exe ... server.py` 不是两套彼此独立的后端，而是 Windows `venv` 的父子进程表现；实际只有子进程监听端口，且父进程退出后子进程与监听会一起退出。本条不再作为当前前序功能调试阻塞项。

### 75. 录音流与悬浮窗用户可见中文文案 UTF-8 回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修正 `tauri-app/src/lib/recordingFlow.ts` 中的过短录音提示、开始/取消/完成 toast 与说话人默认占位文案
  - 修正 `tauri-app/src/components/RecordingOverlay.tsx` 中的取消、完成、正在转录、已取消录音文案
  - 修正 `tauri-app/src-tauri/src/lib.rs` 中的托盘菜单中文文案
  - 在 `tauri-app` 下执行 `npm run build`
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
- 结果: 通过
- 说明: 当前录音流 toast、悬浮窗按钮/状态与托盘菜单中的中文文案已恢复为正常 UTF-8，`npm run build` 与 `cargo check` 均通过。该条已完成静态回归；真实桌面端显示效果仍待人工验收确认。

### 76. `feature-rt-history-hotkey` 专题文档中文恢复验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 对比 `git show 48c4d37:docs/archive/phase1/feature-rt-history-hotkey/2026-03-28-rt-history-hotkey-requirements.md`
  - 对比 `git show 48c4d37:docs/archive/phase1/feature-rt-history-hotkey/2026-03-28-rt-history-hotkey-spec.md`
  - 以 `48c4d37` 的正常中文正文为基线重建两份文档
  - 将 2026-03-29 的快捷键交互补充与状态机调整重新按 UTF-8 写回
  - 复查文件头部、上游链接与正文前 30 行
- 结果: 通过
- 说明: 当前 `requirements` 与 `spec` 已恢复为正常中文，且保留了 2026-03-29 的最新快捷键口径补充。该条属于文档基线修复，不涉及代码改动。

### 77. 录音开始到转录入口的端到端日志验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 为 `beginRecordingSession / showOverlay / overlay-state / finishRecordingSession / transcribeAudio / /transcribe / ensure_engine_loaded` 补充链路日志
  - 重启桌面端与后端
  - 按“开始录音 -> 显示悬浮窗 -> 停止录音 -> 模型懒加载 -> 历史记录入库”流程回归
- 结果: 待执行
- 说明: 本条用于明确“热键未触发”“overlay 首帧丢失”“停止后未进转录”各自的真实断点。在日志结果写回前，不对这条链路下结论。

### 78. 录音悬浮窗首次显示日志断点验证（ACL）
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `recordingFlow.ts`、`overlayWindow.ts`、`RecordingOverlay.tsx`、`backend.rs`、`server.py` 补齐开始录音到 `/transcribe` 的链路日志
  - 执行 `npm run build`
  - 执行 `cargo check`
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 查询 `GET http://127.0.0.1:8765/health`
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 失败
- 说明: 热键与开始录音链路已经命中，日志确认 `single_click -> start -> emit_event delivered_to=main -> beginRecordingSession startRecording success`；真实断点出现在 overlay 页面监听权限，日志为 `frontend overlay bind failed: Command plugin:event|listen not allowed by ACL`。当前“第一次点击没反应 / 只出现米黄色空框”应先按 capability 缺失处理，而不是继续误判为热键问题。
### 79. overlay ACL 修复后自动回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修复 `tauri-app/src-tauri/capabilities/main.json`，将 capability 窗口扩展到 `main` 与 `overlay`
  - 修复 `RecordingOverlay.tsx` 的用户可见中文标签
  - 执行 `npm run build`
  - 执行 `cargo check`
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明: 日志已从 `frontend overlay bind failed: Command plugin:event|listen not allowed by ACL` 变为 `frontend overlay bind success` 与 `frontend overlay-ready emitted to main`，说明 overlay 监听权限已经恢复。

### 80. 停止后 FunASR 懒加载链路自动验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 生成 1.2 秒 16kHz mono wav：`.tmp-tests\funasr-smoke-1p2s.wav`
  - 调用 `POST http://127.0.0.1:8765/transcribe`，参数为 `engine=funasr`、`model=seaco-paraformer`
  - 查询 `GET http://127.0.0.1:8765/health`
- 结果: 通过
- 说明: 虽然合成纯音样本的转录结果为空文本，但接口调用成功，且 `/health.loaded_engines.funasr.model=seaco-paraformer`，说明后端 `/transcribe -> ensure_engine_loaded(...)` 的懒加载链路已被真实触发。
### 81. overlay 透明根层与真实 PCM 波纹静态验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修改 `overlay.html`，为 `html` 根节点添加 `overlay-window` 类
  - 修改 `globals.css`，将透明约束扩展到 `html.overlay-window + body.overlay-window + #overlay-root`
  - 修改 `audio.rs`，将真实 `audio-chunk` 同时发送到 `main` 与 `overlay`
  - 修改 `RecordingOverlay.tsx`，使用真实 PCM 数据块计算镜像波纹，`audio-level` 仅作回退
  - 执行 `npm run build`
  - 执行 `cargo check`
- 结果: 通过
- 说明: 当前代码层面已经去掉浅色背景板根因，并把 overlay 波纹切换为真实 PCM 驱动；最终视觉效果仍待桌面端人工验收确认。
### 82. 快捷键开始录音与说话人模型加载误判链路日志定位
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
  - 对照 `hotkey-start-recording`、`beginRecordingSession startRecording success`、`hotkey-stop-recording`、`finishRecordingSession`、`backend transcribe request` 的时间顺序
- 结果: 通过
- 说明: 第一次开始录音实际上已经成功；说话人分离/识别模型只会在后续重复按键触发停止与 `/transcribe` 后才进入。当前问题应按“首帧反馈不足导致误触 stop”处理，而不是按“开始录音同步加载模型阻塞”处理。
### 83. overlay-ready 启动前移自动验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修改 `overlayWindow.ts`，在主窗口模块初始化阶段预注册 `overlay-ready` 监听
  - 修改 `AppShell.tsx`，启动时调用 `primeOverlayBridge()`
  - 执行 `npm run build`
  - 执行 `cargo check`
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明: 冷启动后日志已出现 `frontend overlay bind success -> frontend overlay-ready emitted to main -> frontend overlay-ready received`，说明主窗口不再等到第一次 `showOverlay()` 才注册 ready 监听。
### 84. `Right Alt` 单键热键重复触发日志定位
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 确认桌面端与后端为最新重启进程
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
  - 对照同一次 `Right Alt` 按压下的 `single_click -> start`、`hotkey-start-recording` 与 `beginRecordingSession startRecording success/failed`
- 结果: 通过
- 说明: 当前“第一次按快捷键不稳定”已确认不是旧进程未重启，也不是开始阶段同步加载说话人模型，而是 `Right Alt` 在热键层对同一物理按压重复发出了开始事件。
### 85. `Right Alt` 单键热键去重修复与完整重启验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修改 `tauri-app/src-tauri/src/commands/hotkey.rs`，为同名 `hotkey-start-recording` / `hotkey-stop-recording` 增加 200ms 去重
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
  - 手动结束旧的 `voicescribe-desktop.exe` 与 `server.py` 相关进程
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 查询 `GET http://127.0.0.1:8765/health`
  - 查询新进程启动时间
- 结果: 通过
- 说明: `cargo check` 通过；完整重启后新进程时间更新为桌面端 2026-03-29 23:19:21、后端 2026-03-29 23:19:23，说明当前正在运行的是本轮修复后的新版本。`Right Alt` 物理按压内重复 `start` 是否已完全消失，还需用户手动按一次热键继续确认。
### 86. 外接键盘 `Right Alt` 热键运行时键码不一致定位
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 完整重启桌面端与后端
  - 用户明确使用外接键盘右 Alt 单击一次
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
  - 读取 `C:\Users\DingK\AppData\Roaming\com.voicescribe.desktop\voicescribe-settings.json`
- 结果: 失败
- 说明: 当前设置文件保存的是 `Right Alt / AltRight / 165`，但本次真实按键日志只出现 `vk=164`，没有 `matches_hotkey`。问题已定位为外接键盘右 Alt 在设置页保存值与运行时低层 hook 键码不一致。
### 87. 外接键盘右 Alt 扩展标志匹配修复与重启验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修改 `tauri-app/src-tauri/src/commands/hotkey.rs`，将右 Alt 匹配改为 `vk=165` 或 `vk=164 + extended flag`
  - 同时补充热键原始日志的 `scanCode` 与 `flags`
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
  - 手动结束旧的 `voicescribe-desktop.exe` 与 `server.py` 相关进程
  - 执行 `cmd /c scripts\start_windows_system.bat`
  - 查询 `GET http://127.0.0.1:8765/health`
- 结果: 通过
- 说明: 代码编译通过，完整重启后新进程时间更新为桌面端 2026-03-29 23:29:50、后端 2026-03-29 23:29:52。真实外接键盘右 Alt 是否已命中，仍待用户回归。### 88. 右 Alt 扩展标志匹配未落盘与空录音 diarization 500 定位
- 执行目录: D:\learn\AIGC\voicescribe\0324\voicescribe
- 执行方式:
  - 读取最新 
oicescribe-hotkey.log
  - 回读 	auri-app/src-tauri/src/commands/hotkey.rs
  - 对照 ackend/diarization/speaker.py 与 /transcribe 日志
- 结果: 失败
- 说明: 当前热键源码仍是旧版 
k == primary_key_code 判定，导致 
k=164 + extended flag 的外接键盘右 Alt 不命中；同时空录音/无效语音进入说话人分离会抛 500，并引发前端重复重试。
### 89. 快捷键“首次可用、后续同一物理键经常失配”日志定位
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 冷启动桌面端与后端
  - 用户重新录制并保存当前热键
  - 真实执行一轮“开始录音 -> 停止录音 -> 再次按同一物理键”
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 失败
- 说明: 第一轮录音链路可正常命中 `matches_hotkey -> beginRecordingSession startRecording success -> hotkey-stop-recording -> /transcribe 200`；但后续失效按键大量表现为 `raw_modifier_event vk=164 scan=56 flags=32/128`，只有 `hotkey_candidate` 没有 `matches_hotkey`。当前已确认不是默认热键回退，也不是录音状态未复位，而是运行时物理键事件与保存键码不稳定一致。

### 90. 空录音 / 无有效语音进入说话人分离的异常链路定位
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 启用说话人分离
  - 执行真实短录音或无语音录音
  - 检查后端 `/transcribe` 日志与前端热键日志
- 结果: 失败
- 说明: 当前后端会在 `backend/diarization/speaker.py` 的 `self.diarization_model(processed_audio_path)` 处抛出 `AssertionError: modelscope error: The effective audio duration is too short.`，随后 `/transcribe` 返回 `500`；前端 Rust `transcribe` 命令把该 500 继续作为可重试错误处理，导致重复请求循环。该问题尚未修复。

### 91. 快捷键运行时命中逻辑迁移到 `primaryCode` 后的编译回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 修改 `tauri-app/src-tauri/src/commands/hotkey.rs`
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
- 结果: 通过
- 说明: 当前运行时热键命中逻辑已从“仅按 `primary_key_code` 比较”替换为“按保存的 `primary_code` 识别物理键”，并同步修正主键属于 Alt 家族时的修饰键归一化。该条仅覆盖编译与静态回归，不代表外接键盘人工验收已完成。

### 92. 空录音 / 静音音频在开启说话人分离时不再返回 500 的后端烟测
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 执行 `cmd /c scripts\\start_windows_system.bat`
  - 执行 `curl.exe -s http://127.0.0.1:8765/health`
  - 生成 `.tmp-tests/silence-6s.wav`
  - 执行 `curl.exe -s -X POST http://127.0.0.1:8765/transcribe -F "audio=@.tmp-tests/silence-6s.wav" -F "engine=funasr" -F "model=seaco-paraformer" -F "language=zh" -F "enable_diarization=true" -F "hotwords=" -F "enable_ai_refine=false"`
- 结果: 通过
- 说明: 当前静音 6 秒音频在 `enable_diarization=true` 下已返回 `200`，结果为 `text=''`、`segments=[]`、`duration=0.0`；`/health` 同时显示 `loaded_engines.funasr.model=seaco-paraformer`、`diarization=true`。这说明空录音/无有效语音已不再炸成 500，也不再触发后端重试链。

### 93. 空录音 bug 人工回归确认
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 用户执行真实空录音/无有效语音场景回归
  - 对照当前桌面端行为确认是否仍出现报错、卡住或后端重试循环
- 结果: 通过
- 说明: 用户已明确反馈“空录音 bug 修复了”。当前该问题可视为代码烟测与人工现象两层都已收口。

### 94. 左右 Alt 人工验证样本存在混淆，当前热键问题需按同侧键重新回归
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 回顾之前的热键人工验证现象
  - 结合用户新补充信息确认录制保存侧与实际按下侧是否一致
- 结果: 待重测
- 说明: 用户补充说明，之前某些场景中“录制保存的 Alt 侧”和“实际回归按下的 Alt 侧”可能不一致，因此此前部分“热键后续失效”的人工现象样本存在混淆。下一轮验收需明确记录为 `Left Alt` 或 `Right Alt`，并保证录制与验证使用同一侧物理键。

### 95. 旧快捷键兼容层删除前的调用面确认
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 全局检索 `registerHotkey`、`register_hotkey`、`hotkeyModifiers`、`hotkeyKeyCode`
  - 确认当前主路径是否仍依赖这些旧接口或旧字段
- 结果: 通过
- 说明: 当前前端主路径已统一使用 `hotkeyBinding` 与 `registerHotkeyBinding(...)`；旧的 `registerHotkey(modifiers, keyCode)` 仅剩接口残留，无实际调用方；`hotkeyModifiers` 与 `hotkeyKeyCode` 仍存在于设置结构中，属于待删除兼容字段。

### 96. 旧快捷键兼容层删除与引用清理验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 删除旧前端 API `registerHotkey(modifiers, keyCode)`
  - 删除 Rust 旧命令 `register_hotkey(...)`
  - 删除 `build_binding_from_legacy(...)`
  - 删除设置结构中的 `hotkeyModifiers`、`hotkeyKeyCode`
  - 删除 `HotkeyState` 中不再使用的 `modifiers/key_code`
  - 全局检索确认旧标识已无残留调用
- 结果: 通过
- 说明: 当前仓库内旧快捷键兼容层已清理完成，主路径只保留 `hotkeyBinding` 与 `registerHotkeyBinding(...)`。

### 97. 删除旧快捷键兼容层后的编译回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app/src-tauri` 下执行 `cargo check`
  - 在 `tauri-app` 下执行 `npm run build`
- 结果: 通过
- 说明: 删除旧快捷键兼容层后，前后端编译均通过，说明当前仅保留 `hotkeyBinding` 路径不会破坏现有工程构建。

### 98. ???????? Windows ?? hook ????????
- ????: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- ????:
  - ?? `tauri-app/src-tauri/src/commands/hotkey.rs`??? `start_hotkey_capture / stop_hotkey_capture` ? `hotkey-capture-complete` ????
  - ?? `tauri-app/src/pages/HotkeySettings.tsx`?????? `keydown/keyup` ?????????? Rust ????????? binding
  - ? `tauri-app/src-tauri` ??? `cargo check`
  - ? `tauri-app` ??? `npm run build`
- ??: ??
- ??: ??????????????? Windows ?? hook ????????????????????????? Alt ????????

### 99. ?? hook ?????????????????
- ????: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- ????:
  - ?? `cmd /c scripts\start_windows_system.bat`
  - ?? `curl.exe -s http://127.0.0.1:8765/health`
- ??: ??
- ??: ?????????????`/health` ?? `healthy`?`mock_mode=false`????????????? `models/`?

### 100. `encoding_guard.py` ?????????????
- ????: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- ????:
  - ?? `python scripts/encoding_guard.py --help`
- ??: ??
- ??: ??????? `scan`?`verify`?`repair` ????????? Windows PowerShell ?? UTF-8 ????????

### 101. `encoding_guard.py` ? `docs/active` ??????? UTF-8 ????
- ????: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- ????:
  - ?? `python scripts/encoding_guard.py verify docs/active`
- ??: ??
- ??: ?? `docs/active` ? 13 ???????? UTF-8 ?????????????????????????????? PowerShell ????????
### 102. 快捷键录制链路收口后的编译与启动回归验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 运行 `npm run build`
  - 运行 `cargo check`
  - 运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
- 结果: 通过
- 说明:
  - 本轮修改覆盖 `tauri-app/src/pages/HotkeySettings.tsx` 与 `tauri-app/src-tauri/src/commands/hotkey.rs`，重点是防止录制中误用旧 binding，并避免后台重新注册热键时静默关闭当前捕获。
  - 当前确认前端构建、Rust 编译、桌面端启动与后端健康检查均正常，`/health` 返回 `healthy` 且 `mock_mode=false`。
  - 这条只覆盖代码层与启动层回归，不代表“录制快捷键”已人工验收通过；`start_hotkey_capture -> hotkey-capture-complete` 的真实键盘回归仍待继续观察热键日志和人工操作确认。

### 103. 快捷键 `keys[]` 模型重构后的构建、启动与注册日志验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app` 下运行 `npm run build`
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
  - 检查 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明:
  - 本轮已将前后端快捷键结构统一为 `HotkeyBinding { keys, display }`，并删除设置页的直接注册入口，改为只通过 store 变更触发 `useHotkey.ts` 注册。
  - 本轮启动后的热键日志已出现新格式记录：`register_hotkey_binding keys=0xA5 display=右 Alt`，说明新 `keys[]` 结构已经进入 Rust 运行时，而不是继续沿用旧 `primaryCode / primaryKeyCode / modifiers` 日志链路。
  - 当前已确认 `npm run build`、`cargo check`、桌面端启动、`/health` 和热键注册日志均正常。
  - 这条仍不代表“快捷键录制问题已人工验收通过”；`start_hotkey_capture -> capture_complete` 的真实录制回归，以及左右 `Alt` 的真实录制与命中验收，仍需继续手动操作并检查热键日志。

### 104. 快捷键页中文文案修正与录制入口副作用收口后的构建回归
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app` 下运行 `npm run build`
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
  - 检查 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明:
  - 本轮把快捷键页中文文案全部收口到字符串常量 / 表达式渲染，避免 JSX 文本节点直接渲染 `\uXXXX` 字面量导致页面乱码。
  - 本轮同时把设置页录制入口从“`capturing` 驱动 effect 自动 `start/stop`”改为显式按钮处理，避免录制依赖 effect cleanup 隐式停止。
  - 当前已确认 `npm run build`、`cargo check`、桌面端启动和 `/health` 均正常，且热键日志仍能看到新的 `register_hotkey_binding keys=...` 记录。
  - 这条仍不代表“点击录制后任意按键已恢复正常”；`start_hotkey_capture -> capture_key_down -> capture_complete` 的真实录制链路还需要继续手动操作和日志确认。

### 105. 快捷键录制 raw capture 日志补充后的 Rust 构建与启动回归
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
- 结果: 通过
- 说明:
  - 本轮只补充了 Rust 侧录制 raw 日志，用于继续定位“开始录制后没有 `capture_key_down / capture_complete`”的真实断点。
  - 当前已确认 `cargo check`、桌面端启动和 `/health` 均正常。
  - 这条仍不代表录制已恢复；需要基于下一次人工点击“开始录制”后的新日志，继续判断是 hook 在录制窗口内没有收到按键，还是按键到了但在 `try_handle_capture(...)` 内被放掉。

### 106. 单监听源分流收口后的构建、启动与新进程确认
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在 `tauri-app` 下运行 `npm run build`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
  - 读取 `Get-Process voicescribe-desktop`
  - 检查 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明:
  - 本轮把 Rust 热键状态流继续收口到“单监听源 + 录制态优先分流”：`start_hotkey_capture / stop_hotkey_capture` 不再顺带清理 runtime 命中状态，录制窗口内的键盘事件也不再继续落到 runtime 分支。
  - 当前已确认 `cargo check`、`npm run build`、`cmd /c scripts\start_windows_system.bat` 与 `/health` 均正常。
  - 当前运行中的新桌面进程为 `voicescribe-desktop`，启动时间为 `2026/3/30 14:39:48`。
  - 热键日志已确认新进程重新写入 `register_hotkey_binding keys=0xA5 ...`，说明 release 可执行文件已更新；但这条仍不代表“录制快捷键已修好”，新的人工录制回归还未完成。

### 107. 快捷键录制最小探针模式的构建与新进程启动验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在 `tauri-app` 下运行 `npm run build`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
  - 读取 `Get-Process voicescribe-desktop`
- 结果: 通过
- 说明:
  - 本轮没有继续修改保存/应用逻辑，只在 Rust 侧加入“最小原始事件探针模式”，用于记录录制开始后 10 秒内 hook 回调看到的 `CaptureState.active` 与原始 `vkCode / scanCode / flags / message`。
  - 当前已确认 `cargo check`、`npm run build`、`cmd /c scripts\start_windows_system.bat` 与 `/health` 均正常。
  - 当前新桌面进程为 `voicescribe-desktop`，启动时间为 `2026/3/30 14:57:03`，对应探针版 release 可执行文件。
  - 这条仍不代表录制已恢复；需要基于新的最小人工操作，检查热键日志中的 `capture_probe ... active=...` 记录，再决定根因是在 hook 事件进入前，还是在 capture 状态可见性上。

### 108. 快捷键录制最小探针模式的人工日志判读
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在探针版新进程 `voicescribe-desktop (2026/3/30 14:57:03)` 中打开快捷键页
  - 点击“开始录制”
  - 手动按一次 `A`
  - 手动按一次右 `Alt`
  - 点击“停止录制”
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 未通过
- 说明:
  - 日志已确认本次人工操作触发了 `frontend capture start requested` 与 `start_hotkey_capture active=true generation=1`，并在 7 秒后触发 `stop_hotkey_capture active=false generation=1`。
  - 但开始录制到停止录制的整个窗口内，没有任何 `capture_probe ...`、`capture_raw_event ...`、`capture_key_down ...` 或 `capture_complete ...`。
  - 探针模式按设计应在开始录制后的 10 秒内记录所有进入 hook 的键盘事件；本次窗口内一条都没有。
  - 当前结论已经收敛到“录制窗口内键盘事件没有进入当前 hook 回调链路”，而不是 `CaptureState.active` 读错、binding 组装错误、保存设置错误或重新注册错误。

### 109. hook 生命周期自检死锁点删除后的构建、启动与日志验证
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在 `tauri-app/src-tauri` 下运行 `cargo check`
  - 在 `tauri-app` 下运行 `npm run build`
  - 强制结束旧的 `voicescribe-desktop.exe`
  - 在仓库根目录运行 `cmd /c scripts\start_windows_system.bat`
  - 运行 `curl.exe -s http://127.0.0.1:8765/health`
  - 读取 `Get-Process voicescribe-desktop`
  - 检查 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 通过
- 说明:
  - 本轮仅删除 `ensure_hook_thread()` 启动路径里两处会自锁的 `log_hook_runtime_status(...)` 调用，不改录制状态机与匹配逻辑。
  - `cargo check` 与 `npm run build` 已通过；`/health` 返回 `healthy`，`mock_mode=false`。
  - 新桌面进程为 `voicescribe-desktop`，启动时间 `2026/3/30 15:28:16`，`Responding=True`。
  - 热键日志已出现完整启动链路：`register_hotkey_binding -> hook_status context=ensure_hook_thread:enter -> ensure_hook_thread: starting -> ensure_hook_thread: SetWindowsHookExW succeeded -> ensure_hook_thread: startup confirmed`。
  - 结论：此前 hook 生命周期自检确实会污染启动检测结果；删除自锁点后，当前新进程的 hook 线程可以正常启动并进入可继续排查的状态。

### 110. hook 线程正常存活条件下的快捷键录制人工日志判读
- 执行目录: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- 执行方式:
  - 在新进程 `voicescribe-desktop (2026/3/30 15:28:16)` 中打开快捷键页
  - 点击“开始录制”
  - 手动按键后点击“停止录制”
  - 读取 `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`
- 结果: 未通过
- 说明:
  - 日志已确认本次录制开始时 hook 线程存活：`hook_status context=start_hotkey_capture:before_ensure slot_present=true thread_id_present=true thread_finished=false capture_active=true capture_generation=1`。
  - 随后的 `ensure_hook_thread` 也明确记录 `existing_slot thread_id_present=true thread_finished=false`，说明这次不是 stale slot 或死线程。
  - 但从 `frontend capture start registered` 到 `frontend capture stop requested by button` 之间，仍然没有任何 `capture_probe ...`、`capture_raw_event ...`、`capture_key_down ...` 或 `capture_complete ...`。
  - 停止录制后，日志又立即恢复出现普通按键的 `capture_skip_inactive ...` 与 `hotkey_state ...`。
  - 当前结论进一步收敛到：录制窗口内键盘事件没有进入当前 `keyboard_hook_proc(...)` 回调链路；这次已经可以排除 hook 启动失败、线程假存活与 `ensure_hook_thread()` 自锁污染结果。

### 111. Hotkey capture-to-apply diagnostic logging build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - This round only added diagnostic visibility for `start capture -> key -> stop/apply -> re-register`; it did not change runtime hotkey semantics.
  - The recorded result is build-level regression only, not manual desktop hotkey acceptance.

### 112. Browser-based settings capture migration build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - Settings-page capture moved from Rust `start_hotkey_capture / stop_hotkey_capture / hotkey-capture-complete` to current-window browser `keydown/keyup` capture.
  - Runtime global hotkey listening still stays on Rust `WH_KEYBOARD_LL`.
  - Manual desktop validation is still pending for `A`, left `Alt`, right `Alt`, `Right Alt + A`, and AltGr behavior.

### 113. Stale pressed-key recovery build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - Rust runtime hotkey matching now prunes stale keys from `pressed_keys` before comparing against the registered binding set.
  - This specifically targets cases where a system-level flow such as `Alt+Tab` leaves a ghost key like `Tab` in the runtime state and blocks later single-key hotkey matches.
  - Manual desktop verification is still required.

### 114. Manual desktop verification after stale pressed-key recovery
- Verification owner: user manual validation
- Time: 2026-03-30
- Result: passed
- Notes:
  - User confirmed that the hotkey start/apply flow is working again after the stale pressed-key recovery change.
  - This entry records manual confirmation only; no additional automated desktop-input test was executed by the agent.

### 115. Observe-only runtime hook build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - Runtime low-level hook now keeps the reference implementation behavior: emit hotkey state transitions without swallowing matched key events from the foreground application.
  - This change preserves the existing runtime state machine and stale-key recovery logic while removing `LRESULT(1)` interception on hotkey match and `Esc` cancel.
  - Manual desktop verification is still required.


### 116. Suspend runtime matching during settings capture build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - Settings-page capture now suspends Rust runtime hotkey matching before browser capture starts, and resumes it when capture ends, cancels, stops, or the page effect cleans up.
  - The saved binding is kept intact; this change only pauses runtime match transitions and clears transient pressed-state during capture.
  - Manual desktop verification is still required: while capture is active, pressing the currently registered hotkey must not start recording; after capture exits, runtime hotkey behavior must resume normally.

### 117. Apply trace timeline diagnostic logging build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - This round adds one shared `trace_id` across settings capture/apply, store-driven re-register, Rust runtime resume, and the first subsequent `hotkey_state`.
  - The goal is only to diagnose where the reported 7-8 second recovery window occurs; runtime hotkey behavior is not intentionally changed in this round.
  - Manual desktop verification is still required by inspecting `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`.

### 118. Apply raw hook-event diagnostic logging build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - This round keeps the same Apply `trace_id`, but extends it into a bounded post-Apply Rust hook diagnostic window.
  - New logs include `hook_raw` with `message / vk / scan / flags / normalized_vk`, plus traced `hotkey_state` entries with the corresponding `pressed_keys` and `binding`.
  - The trace window is bounded by event count and time; this round is still diagnostic only and does not intentionally change runtime hotkey behavior.
  - Manual desktop verification is still required by inspecting `C:\Users\DingK\AppData\Local\Temp\voicescribe-hotkey.log`.

### 119. Startup hotkey trace diagnostics and millisecond hotkey-log timestamp build regression
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `cargo fmt` in `tauri-app/src-tauri`
  - Run `cargo check` in `tauri-app/src-tauri`
  - Run `npm run build` in `tauri-app`
- Result: passed
- Notes:
  - This round extends the hotkey diagnostic model from Apply-only to `startup + Apply`; the first startup listener-bind and first store-driven register now share one startup `trace_id`.
  - Hotkey settings capture now logs explicit `runtime hotkey suspend requested` and `runtime hotkey resume requested` entries before the corresponding invoke calls, so invoke-boundary latency can be separated from Rust runtime latency.
  - Rust shared hotkey log timestamps now use millisecond resolution, and the bounded trace window is widened to 20 seconds to cover cold-start observation plus the reported 7-8 second recovery interval.
  - This is still build-level regression only. No manual desktop hotkey acceptance was executed in this round, so cold-start `Right Alt` and Apply recovery remain `待人工验收`.

### 120. `pyannote-3.1` missing runtime dependency is normalized from backend 500 to explicit 400
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Run `backend/venv/Scripts/python.exe -m compileall backend`
  - Run a targeted Python script that imports `RuntimeTranscriptionService` and calls `ensure_diarization_loaded('pyannote-3.1')`
  - Run a targeted Python script that imports `server._get_model_status('diarization', 'diarization', 'pyannote-3.1')`
- Result: passed
- Notes:
  - Current `backend/venv` was confirmed to lack `pyannote` / `pyannote.audio`; this round does not install the dependency itself.
  - The service-level verification now returns `HTTPException 400` with explicit detail: `pyannote-3.1 requires backend dependency pyannote.audio. Install it in backend/venv before using this diarization model.`
  - Backend model status now exposes the same missing-dependency message on `pyannote-3.1`, instead of leaving the model without a clear runtime error hint.
  - This round verifies error classification and observability only; it does not claim that `pyannote-3.1` can actually run until the dependency is installed and a real diarization request is re-validated.

### 121. `pyannote.audio` pinned install is present, but Windows runtime import is still blocked by `onnxruntime`
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Edit `backend/requirements.txt` to pin `pyannote.audio==3.1.1`
  - Run `backend/venv/Scripts/python.exe -m pip install pyannote.audio==3.1.1`
  - Run targeted repair installs to restore `torch==2.5.1+cu124`, `torchaudio==2.5.1+cu124`, `huggingface-hub==0.36.2`, `fsspec==2026.2.0`, and `protobuf==6.33.6`
  - Run `backend/venv/Scripts/python.exe -c "import torch; print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available())"`
  - Run `backend/venv/Scripts/python.exe -c "import onnxruntime as ort; print(ort.__version__)"`
  - Run `backend/venv/Scripts/python.exe -c "from pyannote.audio import Pipeline; print('ok')"`
- Result: partial
- Notes:
  - `backend/requirements.txt` now pins `pyannote.audio==3.1.1`, matching the `speaker-diarization-3.1` compatible line rather than floating to the latest major version.
  - After dependency repair, `torch 2.5.1+cu124` imports successfully and reports `cuda 12.4`, `cuda_available True`.
  - `pyannote.audio 3.1.1` is installed in the current `backend/venv`, but `from pyannote.audio import Pipeline` still fails on this Windows machine because the import chain reaches `torchmetrics -> onnxruntime`, and `import onnxruntime` currently fails with `ImportError: DLL load failed while importing onnxruntime_pybind11_state`.
  - This round only proves that the package is pinned and installed. It does not prove that `pyannote-3.1` is runnable yet, so real diarization acceptance remains `待继续排查`.

### 122. `pyannote-3.1` import path is no longer blocked by broken `onnxruntime`, and failed pyannote load is normalized to explicit 400
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Edit `backend/diarization/speaker.py` to mask broken optional `onnxruntime` during `pyannote.audio` import, validate local `pyannote-3.1` model directories more strictly, and reject `None` pipeline objects
  - Edit `backend/services/transcription_service.py` to normalize pyannote load/runtime failures from `ImportError` / `RuntimeError` / `ValueError` to `HTTPException 400`
  - Edit `backend/requirements.txt`, `backend/requirements-core.txt`, and `backend/requirements-minimal.txt` to pin `numpy<2.0.0`
  - Run `backend/venv/Scripts/python.exe -m pip install --force-reinstall "numpy<2.0.0,>=1.24.0"`
  - Run `backend/venv/Scripts/python.exe -m compileall backend/diarization/speaker.py backend/services/transcription_service.py backend/server.py`
  - Run a targeted Python script that verifies `SpeakerDiarizer.pyannote_audio_available()`, `SpeakerDiarizer.onnxruntime_runtime_available()`, and `SpeakerDiarizer._import_pyannote_pipeline()`
  - Run a targeted Python script that calls `RuntimeTranscriptionService.ensure_diarization_loaded('pyannote-3.1')`
- Result: passed
- Notes:
  - `onnxruntime` still cannot be imported on this Windows machine, but that broken optional dependency no longer blocks the `pyannote.audio` import path used by `SpeakerDiarizer`; `_import_pyannote_pipeline()` now returns the `Pipeline` class successfully.
  - Backend requirements now pin `numpy<2.0.0`; current `backend/venv` was downgraded from `numpy 2.4.4` to `numpy 1.26.4`, which removes the `np.NaN` compatibility failure in `pyannote.audio 3.1.1`.
  - `RuntimeTranscriptionService.ensure_diarization_loaded('pyannote-3.1')` now returns `HTTPException 400` with explicit detail when the gated pyannote repo cannot be loaded into a usable pipeline: `pyannote-3.1 pipeline could not be loaded ... Provide a valid Hugging Face token and ensure the pyannote model is downloaded ...`
  - This round verifies that the Windows runtime chain is no longer blocked by `onnxruntime` or `numpy 2.x`. It does not claim real gated-model download or real diarization acceptance, which still require a valid Hugging Face token and completed pyannote model assets.

### 123. Incomplete local `pyannote-3.1` directory no longer appears as downloaded, and stale registry entry is cleared
- Working directory: `D:\learn\AIGC\voicescribe\0324\voicescribe`
- Commands:
  - Edit `backend/diarization/speaker.py` to centralize `pyannote-3.1` complete-directory detection
  - Edit `backend/server.py` so `/models` and HF snapshot download only register `pyannote-3.1` when the local directory contains a complete checkpoint
  - Run `backend/venv/Scripts/python.exe -m compileall backend/diarization/speaker.py backend/server.py backend/services/transcription_service.py`
  - Run a targeted Python script that calls `server._get_model_status('diarization', 'diarization', 'pyannote-3.1')`, inspects `server._get_registry_entry(...)`, and checks a synthetic complete pyannote directory
- Result: passed
- Notes:
  - The current local directory [pyannote-3.1](/D:/learn/AIGC/voicescribe/0324/voicescribe/models/diarization/pyannote-3.1) still exists, but because it lacks `config.yaml` and checkpoint weights, backend model status now returns `available=False` with explicit error: `pyannote-3.1 local model directory is incomplete ...`
  - The stale `models/voicescribe_models.json` entry for `pyannote-3.1` is now removed during status reconciliation instead of being preserved with the old false `size_bytes=1` value.
  - A synthetic directory containing `config.yaml` and a dummy `*.safetensors` file is accepted by `SpeakerDiarizer.pyannote_local_dir_complete(...)`, confirming that the new completeness check does not reject valid pyannote-style local directories.
  - This round verifies state correction and registry self-healing only. It does not claim that the real pyannote model has been fully downloaded yet.
