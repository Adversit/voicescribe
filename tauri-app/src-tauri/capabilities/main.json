{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main and overlay VoiceScribe desktop windows.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "store:default",
    "autostart:default",
    "clipboard-manager:allow-write-text"
  ]
}
